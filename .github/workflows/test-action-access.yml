name: Test action access

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-2022
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4